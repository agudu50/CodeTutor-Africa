# Core FastAPI & ASGI
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.8.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0

# Database & Persistence (Lightweight local SQLite)
sqlalchemy>=2.0.30,<3.0.0
aiosqlite>=0.20.0,<1.0.0
alembic>=1.13.0,<2.0.0

# System Resource Monitoring (ADTC Profiling metrics)
psutil>=6.0.0,<7.0.0

# Optional Local GGUF LLM Runtime (Installed on target runtime)
# llama-cpp-python>=0.3.0; sys_platform == 'linux' or sys_platform == 'win32'

# Testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
httpx>=0.27.0,<1.0.0
